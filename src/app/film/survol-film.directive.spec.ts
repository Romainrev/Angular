import { SurvolFilmDirective } from './survol-film.directive';

describe('SurvolFilmDirective', () => {
  it('should create an instance', () => {
    const directive = new SurvolFilmDirective();
    expect(directive).toBeTruthy();
  });
});
